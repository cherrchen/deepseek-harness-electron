import { Context } from '@deepseek-ai/cordis'

/** Runtime value that proves Host package imports remain external. */
export const cordisContextConstructor = Context

/** Host half fixture entry. */
export function apply(): void {}
